name: agal

# Disabled: installed personal github.com/lxndrbe/agal and expected
# audiolabs/ output. Re-enable after it targets LX-Audiolabs/agal and agal/.
on:
  workflow_dispatch:

jobs:
  generate:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: disabled
        run: echo "agal workflow disabled"
