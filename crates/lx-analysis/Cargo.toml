[package]
name = "lx-analysis"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
description = "Product analysis façade: aura_dsp::analysis + shm/vault + plugin *Shared"

[features]
dev-logging = ["aura-dsp/dev-logging"]

[dependencies]
aura-dsp = { workspace = true }
aura-shm = { workspace = true }
