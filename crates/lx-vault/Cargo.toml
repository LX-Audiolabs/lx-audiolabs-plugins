[package]
name = "lx-vault"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
authors = ["LX Audiolabs"]
description = "LX product vault: plugin config paths, MD frontmatter, last-preset hints"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
