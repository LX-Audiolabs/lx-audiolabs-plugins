[package]
name = "lx-slint-editor"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
description = "Thin re-export of aura_editor typed layer — LX Audiolabs"
publish = false

[dependencies]
aura-editor = { workspace = true, features = ["backend-femtovg"] }
paste = "1"
