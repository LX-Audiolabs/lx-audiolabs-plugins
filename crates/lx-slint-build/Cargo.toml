[package]
name = "lx-slint-build"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0 OR MIT"
description = "Build helper: AURA fonts + @aura widgets via aura-build"
publish = false

[dependencies]
aura-build = { workspace = true }
